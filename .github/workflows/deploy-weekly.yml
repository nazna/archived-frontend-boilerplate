name: deploy-weekly

on:
  schedule:
    - cron: '0 0 * * 5'

env:
  CI: true
  TZ: Asia/Tokyo

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - run: npm install
      - run: npm run build
      - run: vercel deploy dist --prod --no-clipboard --token=$TOKEN
        env:
          TOKEN: ${{ secrets.VERCEL_TOKEN }}
